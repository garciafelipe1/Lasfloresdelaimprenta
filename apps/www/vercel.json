{
  "framework": "nextjs",
  "installCommand": "cp ../../pnpm-lock.yaml ./ && corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next"
}

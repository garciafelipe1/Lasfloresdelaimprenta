{
  "framework": "nextjs",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm install --frozen-lockfile && cd apps/www",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next"
}

{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter ./apps/www build",
  "outputDirectory": "apps/www/.next"
}
